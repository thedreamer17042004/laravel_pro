<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class PermissionController extends Controller
{
    public function index() {
        // $routes = [];

        // $all = Route::getRoutes();

        // foreach($all as $rout) {
        //     $name = $rout->getName();
        //     $check = strpos($name, 'admin');
        //     if($check !== false){
        //         array_push($routes, $rout->getName());
        //     }
        // }
    }
}
